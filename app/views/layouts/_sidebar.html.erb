

  <!-- -->
  <div class="sidebarblock">
    <h3>Categories</h3>
    <div class="divline"></div>
    <div class="blocktxt">
      <ul class="cats">
        <% Category.all.each do |cat|%>
        <li><a href=<%= category_path(cat) %>><%= cat.name %> <span class="badge pull-right"><%= cat.posts.count %></span></a></li>
        <%end%>

      </ul>
    </div>
  </div>

  <!-- -->
  <div class="sidebarblock">
    <h3>Topics</h3>
    <div class="divline"></div>
    <div class="blocktxt">
      <ul class="cats">
        <% Topic.all.each do |topic| %>

        <li><a href="#"><%=topic.name%><span class="badge pull-right"><%= topic.posts.count %></span></a></li>

          <%end%>
      </ul>
    </div>
  </div>

  <!-- -->



